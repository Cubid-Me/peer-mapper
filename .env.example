# Global secrets and configuration
MOONBEAM_RPC=https://rpc.api.moonbeam.network
MOONBASE_ALPHA_RPC=https://rpc.api.moonbase.moonbeam.network
PRIVATE_KEY_RELAYER=
PRIVATE_KEY_DEPLOYER=

# Schema configuration (RegisterSchema.s.sol)
SCHEMA_REVOCABLE=true

# Note: Contract addresses are managed in addresses.json
# Deploy scripts automatically read from and write to addresses.json
